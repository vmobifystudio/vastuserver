<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security 
		http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/util 
		http://www.springframework.org/schema/util/spring-util.xsd">

	<http pattern="/assets/**" security="none" />
	<global-method-security secured-annotations="enabled" />

	<http pattern="/restapi/*" create-session="stateless" realm="vastu API" use-expressions="true" path-type="regex">
		<intercept-url pattern="/login.*" />
		<http-basic />
		<access-denied-handler error-page="/restapi/denied" />
	</http>

	<!-- <http pattern="/deviceapi/**" create-session="stateless" realm="MySkool 
		Device API"> <intercept-url pattern="/**" access="ROLE_DEVICE" /> <http-basic 
		/> <access-denied-handler error-page="/restapi/denied"/> </http> -->

	<http auto-config="true" use-expressions="true" path-type="regex">
		<intercept-url pattern="/login.*" access="permitAll" />
		<intercept-url pattern="/logout" access="permitAll" />
		<intercept-url pattern="/register/*" access="permitAll" />
		<intercept-url pattern="/api_test.*" access="permitAll" />
		<intercept-url pattern="/home" access="isAuthenticated()" />
		<intercept-url pattern="/" access="isAuthenticated()" />
		<intercept-url pattern="/denied" access="isAuthenticated()" />
		<intercept-url pattern="/user/.*"
			access="hasAnyAuthority('ROLE_SUPER_ADMIN', 'ROLE_BUSINESS_USER')" />
		<intercept-url pattern="/.*" access="permitAll" />
		<form-login login-page="/login" always-use-default-target="true"
			authentication-failure-url="/login/failure" default-target-url="/home" />

		<access-denied-handler error-page="/denied" />

		<logout invalidate-session="true" delete-cookies="JSESSIONID"
			logout-success-url="/logout/success" logout-url="/logout" />

	</http>

	<authentication-manager>
		<authentication-provider user-service-ref="customUserDetailsService">
			<password-encoder hash="md5" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>